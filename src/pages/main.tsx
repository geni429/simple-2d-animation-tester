import * as React from "react";

import { AsideTool } from "@components";

export class Main extends React.Component {
  render() {
    return (
      <React.Fragment>
        <AsideTool />
      </React.Fragment>
    );
  }
}
